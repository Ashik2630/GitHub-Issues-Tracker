

### 1️⃣What is the difference between var, let, and const?
Answer: The difference between these three is that the first one is declared globally and can be accessed from anywhere, which sometimes causes a lot of problems in work, but in the case of bar and let, both of them maintain a fixed score within their declaration, and in the case of var, it is reassignable and reaclarable, but in the case of let and const, let is reassignable but not reaclarable, and in the case of const, it works only in a specific area, in that case it is reassignable and reaclarable. So when we are going to change the value of any variable, it is best to use let, and when there will be no change and there will be a fix, it is best to use cont, and bar should not be used.
### 2️⃣What is the spread operator (...)?
Answer:The spread operator is a function in JavaScript that can be used to copy an object from another object and create a new object. It is very useful and is very useful in adding new objects to the copied object. It is very important and makes the work much easier.

### 3️⃣What is the difference between map(), filter(), and forEach()?
Answer:Map Filter and ForEach are Map which returns all the elements from an array into a new array and Filter which extracts the values ​​according to a condition and returns a new array and the forEach is  returns the first element it gets from it.

### 4️⃣What is an arrow function?
Answer:Arrow functions are also a short form of JavaScript in which the arrow symbol can be used very easily in many places to make work much more efficient.

### 5️⃣What are template literals?
Answer: template literals are very powerful one javascript Object because we are more data access time is very useable and very useful and very easy to access more data that's why completing is very useful and very powerful in JavaScript
 





